import {useLocation, useNavigate, useParams} from 'react-router-dom';
const OldCampaign = () => {
    const navigate = useNavigate();
    const { state: { data } = {} } = useLocation();

    return (
        <>
            
        </>
    )
};
export default OldCampaign;